#version 330 core
in vec3 ourColor;
in vec2 ourTexCoord;

out vec4 color;
uniform sampler2D mirrorTex;
uniform sampler2D refractionTex;

void main()
{
	color = mix(texture(mirrorTex, ourTexCoord), texture(refractionTex, ourTexCoord), 0.2);

}